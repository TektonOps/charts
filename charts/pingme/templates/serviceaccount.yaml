{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "pingme.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels: {{ include "pingme.labels" . | nindent 4 }}
{{- end }}
